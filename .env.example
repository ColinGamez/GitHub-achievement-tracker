# ============================================================
# GitHub Activity Orchestrator — Environment Configuration
# ============================================================
# Copy this file to .env and fill in the values.
#
# REQUIRED
# --------
# GITHUB_TOKEN: A GitHub Personal Access Token (classic or fine-grained)
#   Required scopes (classic): repo, workflow
#   Required permissions (fine-grained): Contents (read/write),
#     Issues (read/write), Pull requests (read/write), Metadata (read)
GITHUB_TOKEN=ghp_xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx

# GITHUB_OWNER: The GitHub user or organisation that owns the target repo.
GITHUB_OWNER=ColinGamez

# GITHUB_REPO: The repository name (NOT the full URL).
GITHUB_REPO=GitHub-achievement-tracker

# ============================================================
# OPTIONAL
# ============================================================

# CO_AUTHOR_NAME / CO_AUTHOR_EMAIL:
# Used to add "Co-authored-by" trailers to commits.
# This enables the Pair Extraordinaire achievement.
# Leave blank to skip co-authored commits.
CO_AUTHOR_NAME=
CO_AUTHOR_EMAIL=

# BRANCH_PREFIX: Prefix for auto-created branches (default: "orchestrator/")
BRANCH_PREFIX=orchestrator/

# MAX_ISSUES_PER_RUN: How many issues to create per execution (default: 1)
MAX_ISSUES_PER_RUN=1

# MAX_PRS_PER_RUN: How many PRs to open per execution (default: 1)
MAX_PRS_PER_RUN=1

# AUTO_MERGE: Whether to automatically merge PRs (default: true)
AUTO_MERGE=true

# YOLO_MODE: Merge without review (enables YOLO achievement) (default: false)
YOLO_MODE=false

# ANALYTICS_PATH: Where to store analytics JSON (default: ./data/analytics.json)
ANALYTICS_PATH=./data/analytics.json

# LOG_LEVEL: Logging verbosity — debug | info | warn | error (default: info)
LOG_LEVEL=info
